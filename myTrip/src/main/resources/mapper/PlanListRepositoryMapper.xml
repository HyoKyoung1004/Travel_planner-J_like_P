<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.trip.project.dto.plan.PlanListRepository">

<insert id="planAdd" parameterType="PlanList">
		INSERT INTO PLANLIST(PNUM,content_id,plan_id)
		VALUES (#{pnum},#{content_id},#{plan_id})
</insert>
</mapper>